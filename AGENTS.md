# 维护

榨取能力，不堆治理。Content-Studio 一类工作室容易把流程写成操作系统；这里只留四条闸。过闸的才配进仓。

## 1. 树

`topics/<主题>/` 一棵树：总图、叶子、`sources.md`。叶子一篇一个机制。新事实并进旧叶，不新开「有感」。主题死了，整枝剪掉，不留纪念品。

## 2. 出处先于正文

能写成数字、年份、法令的，先入 `sources.md`（主键、表述、一手链接、访问日期）。正文只许引用账本里的条。账本没有的，写成判断，不许扮成统计。

子代理可以写叶子，不许发明数字。总图和账本留在主上下文。验货用没写过这篇的眼睛，对账本抽查链接。

## 3. 散文，不是聊天，也不是报告

润物细无声。短段推进，少标题腔、少表格、少「这一刀砍在」。给父母、给青年可以第二人称，像写信，不像微信。不把读者按进评论区，不喊一起改变。

禁止为公平而加对冲段。不写具体违法出境步骤。不写救国方案。

## 4. 并行

假边先砍再加人。并行只写不同文件。串行尾是：总图、账本、收束到两条私路、链接是否还活。

读完任何一篇，应能回到该主题总图里的私人选择，而不是留在情绪里。
